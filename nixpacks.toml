[phases.setup]
nixPkgs = ["nodejs_22", "npm-11_x", "caddy"]

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "caddy file-server --listen :${PORT:-8080} --root dist"
